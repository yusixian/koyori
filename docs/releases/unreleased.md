# 未发布

当前开发版本：以根 `package.json` 为准。尚无公开发行安装包。

## 首个工程里程碑

已建立 macOS 桌面工作台、Claude Code / Codex 用户选择目录的只读 Skills 清单、共用核心 CLI、独立文档站和 CI 配置。

- 来源选择与保存、扫描取消、搜索筛选和纯文本预览。
- 授权根内软链接、越界提示、解析失败提示和读取上限。
- 独立文档、搜索、下载状态和更新说明页面。
- 唯一产品版本、本地 DMG/ZIP 候选、校验摘要与打包后验收入口。

Agent 与个人服务页面标示为尚未接入；当前没有模型调用、Bot 连接、语音、使用次数采集、整理写入或自动更新。安装包是本地验收候选，不表示上述完整 0.1 已交付。

本地已通过类型与构建检查、12 项核心/CLI 测试，以及真实 Electron 和打包后应用的只读扫描、预览与来源保存流程。远端 CI 结果以仓库 Actions 为准。
