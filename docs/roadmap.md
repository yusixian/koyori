# 路线图与分发

> 2026-09-22；文档、版本和分发已前移至工程起步，具体功能按原型收敛，不是交付时间承诺。

当前正在审阅 [0.1 MVP 草案](design/mvp-0.1.0.md)，扩大为 Skills 管理与个人 Agent 两条主线，包含可恢复整理、cos-tool-bot 服务端接入、证据与操作卡；语音深度待确认。方向已获准，首个工程里程碑已启动，按 M0–M6 和多次 alpha 推进。下列阶段保留长期能力演进，首版准确范围以草案为准。

## 长期能力演进（不替代首版里程碑）

0. 工程与发布基线：最小桌面壳、唯一版本源、检查脚本与 CI、Dokploy 文档站、下载和更新说明、本地安装包；公开发布前完成许可证、安全检查及产物验收。与第一条功能切片同步建设。
1. 资源基础：Electron、基础 CLI、五类资源扫描、自动发现、预览搜索、来源与作用域；先验证一个客户端的 Skill 历史导入、调用统计与覆盖缺口，再扩展另一客户端。
2. 自用闭环：编辑/diff/恢复、MCP 配置启停、Skill/command 项目部署、缓存占用与清理；加入闲置/到期复查规则、整理建议、保留偏好与定期摘要。其中首版选定的 Skills 能力按草案验收，其余按需后续加入。
3. 持续 preview：复用起步阶段的安装包、文档与发布链，完善反馈、社区及更新入口。自动更新在签名与升级路径验证后开放，不把开发模式启动当作安装包验收。
4. AI 增强：在首版个人 Agent、Bot 接入与 Skills 建议之上，继续评估 ACP、更多协议、提示词优化和更丰富的工作台工具。
5. 效果与自动规则：相同任务试验、任务预设、经授权的受限自动应用。
6. 商业验证：个人专业能力、可选云服务、团队共享；根据真实需求选择，不提前建设。

不频繁跑全套测试，不追求覆盖率。里程碑做相关类型/构建和真实流程检查；修改写入/删除/恢复边界时做针对性验证。前期不维护多代内部兼容，但真实资源和应用持久数据始终受保护。

[Skills 使用统计与整理建议](skill-lifecycle.md) 按“本地账本 → 可解释规则 → AI 顾问”推进。扩大版 0.1 提议同时包含机械提醒与按需 AI 解释；第 5 阶段的自动规则指经授权执行操作，不是基础定期检查。候选阈值和调用归因须经样本验证，不承诺完整历史总量。

## 产品标配

- 反馈入口：Issue 模板或表单，诊断摘要先预览，默认不包含项目内容和会话。
- 社区：先链接 GitHub Discussions；私有代码需要对外反馈时可另建公开 community 仓库。
- 更新日志：官网与应用引用同一发布内容。
- 更新：手动/后台检查，preview/stable 渠道，下载与错误状态；重启不能中断配置写入与 Agent 任务。
- CLI：基础查询/扫描/计划与 MVP 同步，后续补齐已验证的执行接口。AI 不通过另一套逻辑改配置。

## 官网和分发

同一个 monorepo，apps/site 独立构建并部署到 Dokploy，承载官网、/docs、/download、/changelog 及反馈/社区入口。Dokploy 是已确认的部署平台；域名、站点框架和具体应用配置仍待确定，当前未部署。详见 [发布与开源基线](design/release-foundation.md)。

安装产物优先考虑公开 GitHub Releases，需要时扩展对象存储/CDN，不进入站点镜像或临时构建目录。用户已确定从首版开源；公开前仍需选择许可证、检查历史并配置无需客户端私有仓库 token 的分发源。下载/更新子域可固定入口，底层可替换。

打包器/updater 配套选择；参考 Lody 的 tag 版本统一、签名凭据隔离、原生平台构建和产物元数据匹配，不照搬完整 CI。先构建实际支持的 macOS 架构。macOS 签名/公证和更新验证未具备时，明确只支持手动更新。

发布顺序：不可变产物 → 下载与签名检查 → 更新渠道元数据。保持旧下载入口，能够停止坏版本分发；站点回滚不代表客户端/数据库可降级。CI 和 Dokploy 的外部动作在实施时按用户授权执行。

## 商业与开源

候选收费价值：个人高级自动化/评测，云同步/托管历史，团队共享规则/审核/审计。基础本地访问、导出、恢复和清缓存建议保持可用；不因订阅失效删除资源。

借鉴 Lody 的客户端与托管服务边界。未来计费/账户后端需要独立权限时另建私有服务仓库，公共契约连接；本地核心不依赖其存在。

已确定从首版开源；许可证、品牌使用范围、买断/订阅与具体套餐尚未决定。当前不增加登录、遥测、支付、云数据库或 CRDT。

## 接下来需要决定

- 初始化工具链：包管理器、Electron 构建/打包方案、SQLite 驱动与站点框架。
- 核心进程如何由 GUI/CLI 共同发现并协调生命周期。
- 第一条端到端流程及可控的 synthetic fixture。
- 首次发布架构、签名身份、更新源；域名在部署前确定即可。
